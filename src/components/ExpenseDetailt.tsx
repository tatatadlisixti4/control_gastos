import {Expense} from "../types"

type ExpenseDetailtProps = {
    expense: Expense
}

export default function ExpenseDetailt({expense}: ExpenseDetailtProps) {
    return (
        <div>ExpenseDetailt</div>
    )
}
